# Platform metadata for OpenAI/Codex launch surfaces. The skill runtime does not
# read this file as an execution input.
interface:
  display_name: "에이전트 팀"
  short_description: "5역할 순차 실행 AI 기능 개발 워크플로우"
  icon_small: "./assets/agent-team-small.svg"
  default_prompt: "$agent-team으로 PM, Domain Expert, Architect, Developer, Reviewer 역할을 순차 실행해 AI 기능을 기획·구현·검토해줘."

policy:
  # 명시 호출 전용. `$agent-team`을 사용자가 직접 입력했을 때만 활성화한다.
  allow_implicit_invocation: false
